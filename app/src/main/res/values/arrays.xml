<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      LSPosed 静态作用域。
      只列微信：LSPosed 的作用域选择列表会主动过滤掉模块类应用，
      所以「勾选模块自身」既做不到也不需要 —— v4 的激活自检改成看
      微信侧 Hook 回报的心跳，不依赖自身被注入。
    -->
    <string-array name="xposed_scope">
        <item>com.tencent.mm</item>
    </string-array>
</resources>
